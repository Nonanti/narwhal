# narwhal-schema-diff

Structural diff between two database schemas, plus per-dialect DDL
emission. Powers the `:schema-diff source target` TUI command and the
`narwhal schema-diff` CLI subcommand.

See [`docs/schema-diff.md`](../../docs/schema-diff.md) for the
operator-facing manual and the dialect-by-dialect capability matrix.
